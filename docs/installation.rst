============
Installation
============

At the command line::

    $ easy_install django-smoke-tests

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-smoke-tests
    $ pip install django-smoke-tests
